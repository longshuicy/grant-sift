# Generated state, all of it is rebuildable with `python run.py daily`
grant-sift.db
grant-sift.db-journal
grant-sift.db-wal
grant-sift.db-shm
*.db
*.sqlite
*.sqlite3
web/opportunities.json
run.log
*.log

# Secrets. The gateway key lives in .env and must never be committed.
# .env.example is the documented template and IS tracked, hence the negation.
.env
.env.*
!.env.example
.envrc
secrets.yaml
secrets.yml
*.pem
*.key
credentials.json
helm/**/values-secrets.yaml
helm/**/charts/
!helm/**/Chart.lock

# Deployment roster: real collaborator names/status. Ship the example only.
config/roster.yaml

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/

# Editors and OS
.DS_Store
.idea/
.vscode/
*.swp
