apiVersion: v1
kind: Service
metadata:
  name: {{ include "grant-sift.fullname" . }}
  labels:
    {{- include "grant-sift.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grant-sift.selectorLabels" . | nindent 4 }}
