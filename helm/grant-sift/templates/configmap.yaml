apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "grant-sift.fullname" . }}-config
  labels:
    {{- include "grant-sift.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | toString | quote }}
  {{- end }}
