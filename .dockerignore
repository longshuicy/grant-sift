.git
.venv
venv
__pycache__
*.pyc
*.pyo

# Secrets — never bake into the image build context
.env
.env.*
!.env.example
.envrc
*.pem
*.key
credentials.json
secrets.yaml
secrets.yml
helm/**/values-secrets.yaml
helm/**/values-secrets.yml
config/roster.yaml

*.db
*.db-*
grant-sift.db*
web/opportunities.json
run.log
*.log
.DS_Store
.idea
.vscode
helm/
agent-transcripts
README.md

# Proposals and project documents. Real unfunded/funded proposal text: never
# commit it, never ship it in an image. scripts/build_roster.py reads this
# directory and writes only derived summary lines into the roster.
Projects/
projects/
